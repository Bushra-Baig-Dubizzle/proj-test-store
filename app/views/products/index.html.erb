<h1>All Products</h1>

<ul>
  <% @products.each do |product| %>
    <li>
      ID: <%= product.id %> - 
      Name: <%= product.name.presence || "(no name)" %> - 
      Created: <%= product.created_at.strftime("%Y-%m-%d %H:%M") %>
    </li>
  <% end %>
</ul>
